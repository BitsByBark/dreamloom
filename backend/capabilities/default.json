{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default",
    "fs:allow-open",
    "fs:allow-read-dir",
    "fs:allow-remove",
    "fs:scope",
    {
      "identifier": "fs:allow-read-text-file",
      "allow": [{ "path": "$CONFIG/dreamloom/**" }]
    },
    {
      "identifier": "fs:allow-write-text-file",
      "allow": [{ "path": "$CONFIG/dreamloom/**" }]
    },
    {
      "identifier": "fs:allow-mkdir",
      "allow": [{ "path": "$CONFIG/dreamloom" }, { "path": "$CONFIG/dreamloom/**" }]
    },
    {
      "identifier": "fs:allow-exists",
      "allow": [{ "path": "$CONFIG/dreamloom" }, { "path": "$CONFIG/dreamloom/**" }]
    }
  ]
}
