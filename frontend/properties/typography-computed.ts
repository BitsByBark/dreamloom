import type { TypographyComputedValues } from "./typography-fields";

/** Computed typography from preview — injector will fill this later. */
export function getTypographyComputedValues(): TypographyComputedValues {
  return {};
}
