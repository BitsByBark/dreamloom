import type { BorderComputedValues } from "./border-fields";

/** Computed borders from preview — injector will fill this later. */
export function getBorderComputedValues(): BorderComputedValues {
  return {};
}
